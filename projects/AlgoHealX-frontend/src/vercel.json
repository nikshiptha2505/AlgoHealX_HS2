{
  "version": 2,
  "builds": [
    {
      "src": "projects/AlgoHealX-frontend/src/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
